node_modules/
dist/
.env
*.local

# Deploy build output: the Vite SPA is copied here at deploy time (see DEPLOY.md / Dockerfile)
apps/backend/public/

# Playwright MCP test artifacts
.playwright-mcp/

# Playwright e2e output
test-results/
playwright-report/
playwright/.cache/

# Internal planning / spec artifacts (kept local, not published)
.planning/

.design-handoff
.claude
design-prompts/
auth-design.gz

# Internal planning scratch (kept local, not published)
THE_NEXT_STEP.md
FRIENDS_GROUPS_PLAN.txt