---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/frontend/package.json
  - apps/frontend/tsconfig.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `pnpm install` from the repo root resolves all workspace packages without errors"
    - "TypeScript strict mode is enabled via a shared tsconfig.base.json extended by all packages"
    - "The packages/shared package exports types importable by both backend and frontend without a build step"
    - "`pnpm --filter backend typecheck` and `pnpm --filter frontend typecheck` both complete with zero errors"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace glob declaration activating pnpm monorepo features"
      contains: "apps/*, packages/*"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript config (strict, ES2022, NodeNext)"
    - path: "packages/shared/package.json"
      provides: "Shared types package with name @gambling/shared"
      contains: "@gambling/shared"
    - path: "packages/shared/src/index.ts"
      provides: "Exported shared TypeScript types (ApiResponse, etc.)"
    - path: ".env.example"
      provides: "Committed placeholder env vars"
      contains: "DATABASE_URL"
    - path: ".gitignore"
      provides: "Ensures .env is never committed"
      contains: ".env"
  key_links:
    - from: "apps/backend/package.json"
      to: "packages/shared"
      via: "workspace:* dependency on @gambling/shared"
      pattern: "\"@gambling/shared\": \"workspace:\\*\""
    - from: "apps/frontend/package.json"
      to: "packages/shared"
      via: "workspace:* dependency on @gambling/shared"
      pattern: "\"@gambling/shared\": \"workspace:\\*\""
    - from: "apps/backend/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "extends.*tsconfig.base"
    - from: "apps/frontend/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "extends.*tsconfig.base"
---

<objective>
Set up the pnpm workspace monorepo skeleton: root package.json with dev script, pnpm-workspace.yaml, shared tsconfig.base.json, and all three package scaffolds (apps/backend, apps/frontend, packages/shared) with their tsconfig.json files and minimal package.json files.

Purpose: Every subsequent plan and every subsequent phase builds on this directory and workspace structure. If the tsconfigs are wrong or the workspace protocol is mis-wired, every downstream plan breaks.
Output: A fully configured pnpm workspace where `pnpm install` succeeds, TypeScript is in strict mode, and @gambling/shared types are importable in both apps without a build step.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace configuration</name>
  <files>
    package.json
    pnpm-workspace.yaml
    tsconfig.base.json
    .gitignore
    .env.example
  </files>
  <action>
Create the following files at the repository root (working directory):

**package.json** — root workspace package:
```json
{
  "name": "gambling-website",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"backend,frontend\" -c \"blue,green\" \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "db:migrate": "pnpm --filter backend db:migrate",
    "db:generate": "pnpm --filter backend db:generate",
    "typecheck": "pnpm --filter backend typecheck && pnpm --filter frontend typecheck && pnpm --filter shared typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
```

**pnpm-workspace.yaml**:
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

**tsconfig.base.json** — strict TypeScript base for all packages:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noUncheckedIndexedAccess": true,
    "declaration": true,
    "declarationMap": true
  }
}
```

**.gitignore**:
```
node_modules/
dist/
.env
*.local
```

**.env.example** (committed placeholder — no real secrets):
```
DATABASE_URL=postgres://postgres:postgres@localhost:5432/gambling_dev
PORT=3000
```

After creating these files, run `pnpm install` from the root to initialise the workspace (installs concurrently and typescript at the root).
  </action>
  <verify>
    <automated>cd "C:/Users/Chris/Documents/Coding projects/gambaling website proj" && pnpm install 2>&1 | tail -5</automated>
  </verify>
  <done>
    `pnpm install` exits 0. pnpm-workspace.yaml lists apps/* and packages/*. tsconfig.base.json has strict:true and module:NodeNext. .env is in .gitignore. .env.example exists with DATABASE_URL and PORT placeholders.
  </done>
</task>

<task type="auto">
  <name>Task 2: Package scaffolds — backend, frontend, shared</name>
  <files>
    apps/backend/package.json
    apps/backend/tsconfig.json
    apps/frontend/package.json
    apps/frontend/tsconfig.json
    packages/shared/package.json
    packages/shared/tsconfig.json
    packages/shared/src/index.ts
  </files>
  <action>
Create the three package directories and their configuration files. Then install all dependencies.

**apps/backend/package.json**:
```json
{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gambling/shared": "workspace:*",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "drizzle-orm": "^0.45.0",
    "express": "^4.18.0",
    "helmet": "^8.0.0",
    "pg": "^8.11.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.0",
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.27.0",
    "tsx": "^4.0.0"
  }
}
```

**apps/backend/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*", "drizzle.config.ts"]
}
```

**apps/frontend/package.json**:
```json
{
  "name": "frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gambling/shared": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
```

**apps/frontend/tsconfig.json** — note: must override module/moduleResolution for Vite (NOT NodeNext):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*", "vite.config.ts"]
}
```

**packages/shared/package.json** — points main and types at source .ts file (no build step needed at dev time):
```json
{
  "name": "@gambling/shared",
  "private": true,
  "version": "0.0.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
```

**packages/shared/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "noEmit": true
  },
  "include": ["src/**/*"]
}
```

**packages/shared/src/index.ts** — initial shared types (only types, no runtime values per research recommendation):
```typescript
// Shared TypeScript types for @gambling/shared
// Only export interfaces/types — no runtime values in Phase 1

export interface ApiResponse<T = unknown> {
  success: boolean;
  data?: T;
  error?: string;
}

export interface HealthResponse {
  status: 'ok';
  timestamp: string;
}

// Game types (stubs for future phases)
export type GameType = 'roulette' | 'plinko' | 'mines' | 'blackjack';
```

After creating all package files, run `pnpm install` from the root again to link workspace packages. Then run the typecheck across all packages.
  </action>
  <verify>
    <automated>cd "C:/Users/Chris/Documents/Coding projects/gambaling website proj" && pnpm install && pnpm typecheck 2>&1</automated>
  </verify>
  <done>
    `pnpm install` exits 0 and all three packages are linked. `pnpm typecheck` runs tsc --noEmit on all three packages and reports zero TypeScript errors. The @gambling/shared package exports ApiResponse, HealthResponse, and GameType without a build step.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the following:

1. Directory structure matches the research architecture pattern:
   - gambling-website/ root with pnpm-workspace.yaml, tsconfig.base.json, .env.example, .gitignore
   - apps/backend/, apps/frontend/, packages/shared/ each with package.json and tsconfig.json
   - packages/shared/src/index.ts exists and exports types

2. Workspace integrity:
   ```
   pnpm install  (exits 0, no errors)
   pnpm typecheck  (exits 0, zero TS errors)
   ```

3. .env safety:
   - .gitignore contains ".env"
   - .env.example is committed with placeholder values only

4. Critical constraint: frontend tsconfig.json has `"moduleResolution": "Bundler"` (NOT NodeNext) — this is the most common pitfall for Vite projects per the research.
</verification>

<success_criteria>
- `pnpm install` from root: exits 0, all three workspace packages are resolved
- `pnpm typecheck`: zero TypeScript errors across backend, frontend, and shared
- `packages/shared` is importable as `@gambling/shared` from both apps via workspace:* protocol
- tsconfig.base.json enforces strict mode — no implicit any, no unchecked indexed access
- .env is gitignored; .env.example exists with DATABASE_URL and PORT placeholders
- frontend tsconfig overrides module:ESNext and moduleResolution:Bundler for Vite compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template at @C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</output>
