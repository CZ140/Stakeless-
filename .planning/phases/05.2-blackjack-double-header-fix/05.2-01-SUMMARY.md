---
phase: 05.2-blackjack-double-header-fix
plan: 01
subsystem: api, ui
tags: [blackjack, typescript, react, express, games]

# Dependency graph
requires:
  - phase: 05-remaining-games
    provides: Blackjack backend /blackjack/double endpoint and Header.tsx with BalanceDisplay
provides:
  - POST /blackjack/double returns dealerValue in both bust and normal-completion response paths
  - Header BalanceDisplay rendered as plain element with no navigation side-effect
affects: [frontend blackjack game, header]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Both res.json() call sites in a handler must include all response fields — no field can be omitted from any branch"

key-files:
  created: []
  modified:
    - apps/backend/src/routes/games.ts
    - apps/frontend/src/components/Header.tsx

key-decisions:
  - "dealerValue added after newBalance in both double handler branches (bust and normal completion), mirroring stand handler pattern — minimal change only, no playerValue added"
  - "Link wrapper around BalanceDisplay removed entirely; Link import retained because Logo link still uses it"

patterns-established:
  - "Double handler response shape now matches stand handler: dealerHand + dealerValue always present on completion"

requirements-completed: [BJK-02, BJK-04, AUTH-05]

# Metrics
duration: 2min
completed: 2026-03-03
---

# Phase 05.2 Plan 01: Blackjack Double Header Fix Summary

**Surgical two-line fix: dealerValue added to both /blackjack/double response branches so frontend no longer shows "?" for dealer total, and broken /profile Link wrapper removed from Header BalanceDisplay**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-03T22:08:13Z
- **Completed:** 2026-03-03T22:10:22Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `dealerValue: calculateHandValue(state.dealerHand).value` to both `res.json()` calls in the `/blackjack/double` handler (bust path and normal completion path), matching the existing pattern in the stand handler
- Removed `<Link to="/profile">` wrapper around `<BalanceDisplay />` in Header.tsx — balance display is now a plain non-interactive element
- Both backend and frontend TypeScript compile with zero errors after the changes

## Task Commits

Each task was committed atomically:

1. **Task 1: Add dealerValue to both /blackjack/double response paths** - `8e09b7c` (fix)
2. **Task 2: Remove broken /profile Link wrapper from Header** - `4699fdc` (fix)

**Plan metadata:** (docs commit — see final commit)

## Files Created/Modified
- `apps/backend/src/routes/games.ts` - Added `dealerValue` field to bust-path res.json() and normal-completion res.json() in /blackjack/double handler
- `apps/frontend/src/components/Header.tsx` - Replaced `<Link to="/profile">...<BalanceDisplay />...</Link>` with plain `<BalanceDisplay />`

## Decisions Made
- Only `dealerValue` added to double handler responses — `playerValue` deliberately omitted (frontend DoubleResponse interface does not consume it; minimal-change policy)
- `Link` import kept in Header.tsx — still required for Logo link to `/dashboard`
- No cursor or styling changes to BalanceDisplay itself — per locked user decisions

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None - `pnpm --filter backend exec tsc --noEmit` syntax required instead of `pnpm --filter backend tsc --noEmit` (no tsc script defined in package.json, but tsc binary available via exec). Both backends and frontend compiled cleanly.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Blackjack Double Down defect is closed — dealer hand total displays correctly in post-round results overlay
- Header balance display is non-interactive — no silent navigation to non-existent /profile route
- Phase 05.2 complete; ready for Phase 06 (User Experience Polish: leaderboards, avatars, recent bets history)

---
*Phase: 05.2-blackjack-double-header-fix*
*Completed: 2026-03-03*
